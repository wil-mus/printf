#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 * @parameter: describe the parameter
 * Return: 0 when successfull
 */

int main(void)

{
	int found = 2015, curr = 2022;

	printf("%d\n", found);
	printf("%i\n", curr);

	return (0);
}
