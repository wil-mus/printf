#include "main.h"
#include <stdio.h>
#include <stddarg.h>

/**
 * main - handle the following conversion specifiers
 *
 * Return: 0 when successful
 */

int main(void)
{
	int found = 2015;
	int curr = 2022;

	int found  2015, curr = 2022

	printf("%d\n", found);
	printf("%i\n", curr);

	return (0);
}
